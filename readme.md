## Olá, estou adicionando este 'readme.md' por uma branch.

    Após isso vou criar uma branch a partir desta branch para utilizar a "herança" que as branchs possibilitam

- Sobre:
     O arquivo 'texto.txt' foi adicionado neste repositório a partir de um merge de uma outra branch.

     Este arquivo '.md' também será adicionado por meio de um merge na main.

* git push origin main
* git checkout main

    Está Branch é justamente para ficar diferente da main, e não ter o merge com ela. apenas para teste.
